<%= render "components/select_with_search", {
  label: @label,
  heading_size: "m",
  id: @id,
  name: @name,
  select: {
    multiple: true
  },
  options: @options,
  error_items: @error_items,
  include_blank: true,
} %>
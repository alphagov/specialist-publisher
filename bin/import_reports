#!/usr/bin/env ruby

require File.expand_path("../../config/environment", __FILE__)
require "aaib_import"

# TODO: take these from ARGV
data_files_dir = ARGV.fetch(0)
attachments_dir = ARGV.fetch(1)

AaibImport.call(data_files_dir, attachments_dir)
